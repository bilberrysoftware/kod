# kodit

Kodit is the Kod Integration Tests CLI. This bundles and executes GoLang Cucumber/Gherkin tests for kod.

These are separate from the kod built in tests as they are integration tests that require full systems
to integrate to, rather than the kod CLI's own unit tests which can be ran from an isolated CI environment instead.